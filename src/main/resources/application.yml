# local DB ??
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa_ex1
    username: root
    password: ssh159357!
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: create # ???? create, ???? update. (create? ?? ??? ?? ????? ?? ??? ?)
        default_batch_fetch_size: 1000